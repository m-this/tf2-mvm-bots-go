/* Generated by internal/spbody from the Go it is named after. Do not edit. */

#define MAX_RUNTIME_ATTRIBUTES (20)

#define Go_ItemDefDefault (-1)

bool g_bHasCustomLoadout[65];
bool g_bHasBoughtUpgrades[65];
int m_iWeaponPrimary[65];
int m_iWeaponSecondary[65];
int m_iWeaponMelee[65];
int m_iWeaponPDA2[65];
int m_iAttribPrimary[65][20];
int m_iAttribSecondary[65][20];
int m_iAttribMelee[65][20];
float m_flAttrValPrimary[65][20];
float m_flAttrValSecondary[65][20];
float m_flAttrValMelee[65][20];
int WEAPONS_SCOUT_PRIMARY[31] = {Go_ItemDefDefault, 200, 45, 220, 448, 669, 772, 799, 808, 888, 897, 906, 915, 964, 973, 1078, 1103, 15002, 15015, 15021, 15029, 15036, 15053, 15065, 15069, 15106, 15107, 15108, 15131, 15151, 15157};
int WEAPONS_SCOUT_SECONDARY[27] = {Go_ItemDefDefault, 209, 46, 160, 163, 222, 294, 449, 773, 812, 833, 1121, 1145, 15013, 15018, 15035, 15041, 15046, 15056, 15060, 15061, 15100, 15101, 15102, 15126, 15148, 30666};
int WEAPONS_SCOUT_MELEE[27] = {Go_ItemDefDefault, 190, 44, 221, 264, 317, 325, 349, 355, 423, 450, 452, 474, 572, 648, 660, 880, 939, 954, 999, 1013, 1071, 1123, 1127, 1202, 30667, 30758};
int WEAPONS_SOLDIER_PRIMARY[31] = {Go_ItemDefDefault, 205, 127, 228, 414, 441, 513, 658, 730, 800, 809, 889, 898, 907, 916, 965, 974, 1085, 1104, 15006, 15014, 15028, 15043, 15052, 15057, 15081, 15104, 15105, 15129, 15130, 15150};
int WEAPONS_SOLDIER_SECONDARY[22] = {Go_ItemDefDefault, 199, 129, 133, 226, 354, 415, 442, 444, 1001, 1101, 1141, 1153, 15003, 15016, 15044, 15047, 15085, 15109, 15132, 15133, 15152};
int WEAPONS_SOLDIER_MELEE[19] = {Go_ItemDefDefault, 196, 128, 154, 264, 357, 416, 423, 447, 474, 775, 880, 939, 954, 1013, 1071, 1123, 1127, 30758};
int WEAPONS_PYRO_PRIMARY[31] = {Go_ItemDefDefault, 208, 40, 215, 594, 659, 741, 798, 807, 887, 896, 905, 914, 963, 972, 1146, 1178, 15005, 15017, 15030, 15034, 15049, 15054, 15066, 15067, 15068, 15089, 15090, 15115, 15141, 30474};
int WEAPONS_PYRO_SECONDARY[21] = {Go_ItemDefDefault, 199, 39, 351, 415, 595, 740, 1081, 1141, 1153, 1179, 1180, 15003, 15016, 15044, 15047, 15085, 15109, 15132, 15133, 15152};
int WEAPONS_PYRO_MELEE[26] = {Go_ItemDefDefault, 192, 38, 153, 214, 264, 326, 348, 423, 457, 466, 474, 593, 739, 813, 834, 880, 939, 954, 1000, 1013, 1071, 1123, 1127, 1181, 30758};
int WEAPONS_DEMOMAN_PRIMARY[17] = {Go_ItemDefDefault, 206, 308, 405, 608, 996, 1007, 1101, 1151, 15077, 15079, 15091, 15092, 15116, 15117, 15142, 15158};
int WEAPONS_DEMOMAN_SECONDARY[31] = {Go_ItemDefDefault, 207, 130, 131, 265, 406, 661, 797, 806, 886, 895, 904, 913, 962, 971, 1099, 1144, 1150, 15009, 15012, 15024, 15038, 15045, 15048, 15082, 15083, 15084, 15113, 15137, 15138, 15155};
int WEAPONS_DEMOMAN_MELEE[24] = {Go_ItemDefDefault, 191, 132, 154, 172, 264, 266, 307, 327, 357, 404, 423, 474, 482, 609, 880, 939, 954, 1013, 1071, 1082, 1123, 1127, 30758};
int WEAPONS_HEAVY_PRIMARY[34] = {Go_ItemDefDefault, 202, 41, 298, 312, 424, 654, 793, 802, 811, 832, 850, 882, 891, 900, 909, 958, 967, 1206, 15004, 15020, 15026, 15031, 15040, 15055, 15086, 15087, 15088, 15098, 15099, 15123, 15124, 15125, 15147};
int WEAPONS_HEAVY_SECONDARY[14] = {Go_ItemDefDefault, 199, 425, 1141, 1153, 15003, 15016, 15044, 15047, 15085, 15109, 15132, 15133, 15152};
int WEAPONS_HEAVY_MELEE[22] = {Go_ItemDefDefault, 195, 43, 239, 264, 310, 331, 423, 426, 474, 587, 656, 880, 939, 1013, 1071, 1084, 1100, 1123, 1127, 1184, 30758};
int WEAPONS_ENGINEER_PRIMARY[18] = {Go_ItemDefDefault, 199, 141, 527, 588, 997, 1004, 1141, 1153, 15003, 15016, 15044, 15047, 15085, 15109, 15132, 15133, 15152};
int WEAPONS_ENGINEER_SECONDARY[23] = {Go_ItemDefDefault, 209, 140, 160, 294, 528, 1086, 1202, 15013, 15018, 15035, 15041, 15046, 15056, 15060, 15061, 15100, 15101, 15102, 15126, 15148, 30666, 30668};
int WEAPONS_ENGINEER_MELEE[27] = {Go_ItemDefDefault, 197, 142, 155, 169, 329, 423, 589, 662, 795, 804, 884, 893, 902, 911, 960, 969, 1071, 1123, 15073, 15074, 15075, 15139, 15140, 15114, 15156, 30758};
int WEAPONS_MEDIC_PRIMARY[6] = {Go_ItemDefDefault, 204, 36, 305, 412, 1079};
int WEAPONS_MEDIC_SECONDARY[26] = {Go_ItemDefDefault, 211, 35, 411, 663, 796, 805, 885, 894, 903, 912, 961, 970, 998, 15008, 15010, 15025, 15039, 15050, 15078, 15097, 15121, 15122, 15120, 15145, 15146};
int WEAPONS_MEDIC_MELEE[19] = {Go_ItemDefDefault, 198, 37, 173, 264, 304, 413, 423, 474, 880, 939, 954, 1003, 1013, 1071, 1123, 1127, 1143, 30758};
int WEAPONS_SNIPER_PRIMARY[35] = {Go_ItemDefDefault, 201, 56, 230, 402, 526, 664, 752, 792, 801, 851, 881, 890, 899, 908, 957, 966, 1005, 1092, 1098, 15000, 15007, 15019, 15023, 15033, 15059, 15070, 15071, 15072, 15111, 15112, 15135, 15136, 15154, 30665};
int WEAPONS_SNIPER_SECONDARY[19] = {Go_ItemDefDefault, 203, 57, 58, 231, 642, 751, 1083, 1105, 1149, 15001, 15022, 15032, 15037, 15058, 15076, 15110, 15134, 15153};
int WEAPONS_SNIPER_MELEE[16] = {Go_ItemDefDefault, 193, 171, 232, 264, 401, 423, 474, 880, 939, 954, 1013, 1071, 1123, 1127, 30758};
int WEAPONS_SPY_SECONDARY[20] = {Go_ItemDefDefault, 210, 61, 161, 224, 460, 525, 1006, 1142, 15011, 15027, 15042, 15051, 15062, 15063, 15064, 15103, 15128, 15127, 15149};
int WEAPONS_SPY_BUILDING[7] = {Go_ItemDefDefault, 736, 810, 831, 933, 1080, 1102};
int WEAPONS_SPY_MELEE[29] = {Go_ItemDefDefault, 194, 225, 356, 423, 461, 574, 638, 649, 665, 727, 794, 803, 883, 892, 901, 910, 959, 968, 1071, 15080, 15094, 15095, 15096, 15118, 15119, 15143, 15144, 30758};
int WEAPONS_SPY_PDA2[7] = {Go_ItemDefDefault, 212, 59, 60, 297, 947, 1205};

// ClearSavedAttributes forgets what the last weapon carried.
stock void ClearSavedAttributes(int client)
{
	for (int i = 0; i < MAX_RUNTIME_ATTRIBUTES; i++)
	{
		m_iAttribPrimary[client][i] = 0;
		m_iAttribSecondary[client][i] = 0;
		m_iAttribMelee[client][i] = 0;
		m_flAttrValPrimary[client][i] = 0.0;
		m_flAttrValSecondary[client][i] = 0.0;
		m_flAttrValMelee[client][i] = 0.0;
	}
}

// PrepareCustomLoadout decides what this bot will carry, per class and slot.
//
// The sniper block is mvm-bj8: a stock primary leaves the definition index at
// TF_ITEMDEF_DEFAULT, the classname lookup then fails on an item definition that is
// not one, and the mission was skipped entirely, so the sniper stood where he
// shopped for the rest of the mission. A sniper carrying the default primary is
// carrying the stock rifle: there is nothing to look up and no lookup that can fail.
stock void PrepareCustomLoadout(int client)
{
	switch (TF2_GetPlayerClass(client))
	{
		case TFClass_Scout:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("scout", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("scout", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("scout", "melee", client);
		}
		case TFClass_Soldier:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("soldier", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("soldier", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("soldier", "melee", client);
		}
		case TFClass_Pyro:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("pyro", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("pyro", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("pyro", "melee", client);
		}
		case TFClass_DemoMan:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("demoman", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("demoman", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("demoman", "melee", client);
		}
		case TFClass_Heavy:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("heavyweapons", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("heavyweapons", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("heavyweapons", "melee", client);
		}
		case TFClass_Engineer:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("engineer", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("engineer", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("engineer", "melee", client);
		}
		case TFClass_Medic:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("medic", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("medic", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("medic", "melee", client);
		}
		case TFClass_Sniper:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("sniper", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("sniper", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("sniper", "melee", client);
		}
		case TFClass_Spy:
		{
			m_iWeaponPrimary[client] = GetPreferredWeaponForClass("spy", "primary", client);
			m_iWeaponSecondary[client] = GetPreferredWeaponForClass("spy", "secondary", client);
			m_iWeaponMelee[client] = GetPreferredWeaponForClass("spy", "melee", client);
			m_iWeaponPDA2[client] = GetPreferredWeaponForClass("spy", "pda2", client);
		}
	}
	if (TF2_GetPlayerClass(client) == TFClass_Sniper)
	{
		bool holdingRifle = m_iWeaponPrimary[client] <= Go_ItemDefDefault;
		char itemClassname[512];
		bool named = TF2Econ_GetItemClassName(m_iWeaponPrimary[client], itemClassname, 512);
		if (!holdingRifle && named)
		{
			holdingRifle = StrEqual(itemClassname, "tf_weapon_sniperrifle") || StrEqual(itemClassname, "tf_weapon_sniperrifle_decap") || StrEqual(itemClassname, "tf_weapon_sniperrifle_classic");
		}
		if (holdingRifle)
		{
			SetMission(client, CTFBot_MISSION_SNIPER);
		}
	}
	g_bHasCustomLoadout[client] = true;
}

// ResetLoadouts puts the seat back to the stock items.
stock void ResetLoadouts(int client)
{
	g_bHasCustomLoadout[client] = false;
	m_iWeaponPrimary[client] = Go_ItemDefDefault;
	m_iWeaponSecondary[client] = Go_ItemDefDefault;
	m_iWeaponMelee[client] = Go_ItemDefDefault;
	m_iWeaponPDA2[client] = Go_ItemDefDefault;
}

// ReapplyItemUpgrades puts back what the bot bought, weapon by weapon.
//
// The game hands a bot new weapons on every respawn and they come back stock, so
// what the upgrade station wrote has to be written again. A zero index is the end
// of the list: the rest of the array is the same.
stock void ReapplyItemUpgrades(int client, int primary, int secondary, int melee)
{
	int i;
	if (IsValidEntity(primary))
	{
		for (i = 0; i < MAX_RUNTIME_ATTRIBUTES; i++)
		{
			if (m_iAttribPrimary[client][i] == 0)
			{
				break;
			}
			TF2Attrib_SetByDefIndex(primary, m_iAttribPrimary[client][i], m_flAttrValPrimary[client][i]);
		}
	}
	if (IsValidEntity(secondary))
	{
		for (i = 0; i < MAX_RUNTIME_ATTRIBUTES; i++)
		{
			if (m_iAttribSecondary[client][i] == 0)
			{
				break;
			}
			TF2Attrib_SetByDefIndex(secondary, m_iAttribSecondary[client][i], m_flAttrValSecondary[client][i]);
		}
	}
	if (IsValidEntity(melee))
	{
		for (i = 0; i < MAX_RUNTIME_ATTRIBUTES; i++)
		{
			if (m_iAttribMelee[client][i] == 0)
			{
				break;
			}
			TF2Attrib_SetByDefIndex(melee, m_iAttribMelee[client][i], m_flAttrValMelee[client][i]);
		}
	}
}

// GiveGoldPanStats puts a killstreak on a weapon, drawn rather than chosen.
stock void GiveGoldPanStats(int weapon)
{
	// These may need to become arrays if the effect indexes update in the future.
	int sheen = GetRandomInt(1, 7);
	int killstreaker = GetRandomInt(2002, 2008);
	TF2Attrib_SetByName(weapon, "item style override", 0.0);
	TF2Attrib_SetByName(weapon, "killstreak tier", 3.0);
	TF2Attrib_SetByName(weapon, "killstreak idleeffect", float(sheen));
	TF2Attrib_SetByName(weapon, "killstreak effect", float(killstreaker));
}

// GetRandomWeaponForClass draws one item out of the pool for that class and slot.
//
// The pools are the plugin's own lists, index for index. The spy's are filed one
// slot along from where they are asked for, which is how the plugin has always read
// them.
stock int GetRandomWeaponForClass(const char[] class, const char[] slot)
{
	if (StrEqual(class, "scout", false))
	{
		if (StrEqual(slot, "primary", false))
		{
			return WEAPONS_SCOUT_PRIMARY[GetRandomInt(0, 30)];
		}
		if (StrEqual(slot, "secondary", false))
		{
			return WEAPONS_SCOUT_SECONDARY[GetRandomInt(0, 26)];
		}
		if (StrEqual(slot, "melee", false))
		{
			return WEAPONS_SCOUT_MELEE[GetRandomInt(0, 26)];
		}
	}
	else
		if (StrEqual(class, "soldier", false))
		{
			if (StrEqual(slot, "primary", false))
			{
				return WEAPONS_SOLDIER_PRIMARY[GetRandomInt(0, 30)];
			}
			if (StrEqual(slot, "secondary", false))
			{
				return WEAPONS_SOLDIER_SECONDARY[GetRandomInt(0, 21)];
			}
			if (StrEqual(slot, "melee", false))
			{
				return WEAPONS_SOLDIER_MELEE[GetRandomInt(0, 18)];
			}
		}
		else
			if (StrEqual(class, "pyro", false))
			{
				if (StrEqual(slot, "primary", false))
				{
					return WEAPONS_PYRO_PRIMARY[GetRandomInt(0, 30)];
				}
				if (StrEqual(slot, "secondary", false))
				{
					return WEAPONS_PYRO_SECONDARY[GetRandomInt(0, 20)];
				}
				if (StrEqual(slot, "melee", false))
				{
					return WEAPONS_PYRO_MELEE[GetRandomInt(0, 25)];
				}
			}
			else
				if (StrEqual(class, "demoman", false))
				{
					if (StrEqual(slot, "primary", false))
					{
						return WEAPONS_DEMOMAN_PRIMARY[GetRandomInt(0, 16)];
					}
					if (StrEqual(slot, "secondary", false))
					{
						return WEAPONS_DEMOMAN_SECONDARY[GetRandomInt(0, 30)];
					}
					if (StrEqual(slot, "melee", false))
					{
						return WEAPONS_DEMOMAN_MELEE[GetRandomInt(0, 23)];
					}
				}
				else
					if (StrEqual(class, "heavyweapons", false))
					{
						if (StrEqual(slot, "primary", false))
						{
							return WEAPONS_HEAVY_PRIMARY[GetRandomInt(0, 33)];
						}
						if (StrEqual(slot, "secondary", false))
						{
							return WEAPONS_HEAVY_SECONDARY[GetRandomInt(0, 13)];
						}
						if (StrEqual(slot, "melee", false))
						{
							return WEAPONS_HEAVY_MELEE[GetRandomInt(0, 21)];
						}
					}
					else
						if (StrEqual(class, "engineer", false))
						{
							if (StrEqual(slot, "primary", false))
							{
								return WEAPONS_ENGINEER_PRIMARY[GetRandomInt(0, 17)];
							}
							if (StrEqual(slot, "secondary", false))
							{
								return WEAPONS_ENGINEER_SECONDARY[GetRandomInt(0, 22)];
							}
							if (StrEqual(slot, "melee", false))
							{
								return WEAPONS_ENGINEER_MELEE[GetRandomInt(0, 26)];
							}
						}
						else
							if (StrEqual(class, "medic", false))
							{
								if (StrEqual(slot, "primary", false))
								{
									return WEAPONS_MEDIC_PRIMARY[GetRandomInt(0, 5)];
								}
								if (StrEqual(slot, "secondary", false))
								{
									return WEAPONS_MEDIC_SECONDARY[GetRandomInt(0, 25)];
								}
								if (StrEqual(slot, "melee", false))
								{
									return WEAPONS_MEDIC_MELEE[GetRandomInt(0, 18)];
								}
							}
							else
								if (StrEqual(class, "sniper", false))
								{
									if (StrEqual(slot, "primary", false))
									{
										return WEAPONS_SNIPER_PRIMARY[GetRandomInt(0, 34)];
									}
									if (StrEqual(slot, "secondary", false))
									{
										return WEAPONS_SNIPER_SECONDARY[GetRandomInt(0, 18)];
									}
									if (StrEqual(slot, "melee", false))
									{
										return WEAPONS_SNIPER_MELEE[GetRandomInt(0, 15)];
									}
								}
								else
									if (StrEqual(class, "spy", false))
									{
										if (StrEqual(slot, "primary", false))
										{
											return WEAPONS_SPY_SECONDARY[GetRandomInt(0, 19)];
										}
										if (StrEqual(slot, "secondary", false))
										{
											return WEAPONS_SPY_BUILDING[GetRandomInt(0, 6)];
										}
										if (StrEqual(slot, "melee", false))
										{
											return WEAPONS_SPY_MELEE[GetRandomInt(0, 28)];
										}
										if (StrEqual(slot, "pda2", false))
										{
											return WEAPONS_SPY_PDA2[GetRandomInt(0, 6)];
										}
									}
									else
									{
										PrintToChatAll("[GetRandomWeaponForClass] Unknown class of %s", class);
										LogError("GetRandomWeaponForClass: Unknown class %s", class);
									}
	return -1;
}

// LoadLoadoutFunctions puts the command the bots use on the console.
stock void LoadLoadoutFunctions()
{
	RegConsoleCmd("sm_redbot_upgraded", Command_BoughtUpgrades);
}

// CommandBoughtUpgrades remembers what the upgrade station wrote on this bot's
// weapons.
//
// The bot says so itself: the station is a menu the bot walks through, and this is
// the line it types when it comes out. Only worth remembering with custom loadouts
// on, because that is the only case where the weapons are handed back rather than
// kept.
public Action Command_BoughtUpgrades(int client, int args)
{
	// Only need to remember upgrades if using custom loadouts.
	if (!redbots_manager_use_custom_loadouts.BoolValue)
	{
		return Plugin_Handled;
	}
	// Only our bots should execute this command.
	if (!IsFakeClient(client))
	{
		return Plugin_Handled;
	}
	int primaryWep = GetPlayerWeaponSlot(client, TFWeaponSlot_Primary);
	int secondaryWep = GetPlayerWeaponSlot(client, TFWeaponSlot_Secondary);
	int meleeWep = GetPlayerWeaponSlot(client, TFWeaponSlot_Melee);
	ClearSavedAttributes(client);
	int count;
	Address attr;
	if (IsValidEntity(primaryWep))
	{
		count = TF2Attrib_ListDefIndices(primaryWep, m_iAttribPrimary[client]);
		if (count > 0)
		{
			for (int i = 0; i < count; i++)
			{
				attr = TF2Attrib_GetByDefIndex(primaryWep, m_iAttribPrimary[client][i]);
				m_flAttrValPrimary[client][i] = TF2Attrib_GetValue(attr);
			}
		}
	}
	if (IsValidEntity(secondaryWep))
	{
		count = TF2Attrib_ListDefIndices(secondaryWep, m_iAttribSecondary[client]);
		if (count > 0)
		{
			for (int i = 0; i < count; i++)
			{
				attr = TF2Attrib_GetByDefIndex(secondaryWep, m_iAttribSecondary[client][i]);
				m_flAttrValSecondary[client][i] = TF2Attrib_GetValue(attr);
			}
		}
	}
	if (IsValidEntity(meleeWep))
	{
		count = TF2Attrib_ListDefIndices(meleeWep, m_iAttribMelee[client]);
		if (count > 0)
		{
			for (int i = 0; i < count; i++)
			{
				attr = TF2Attrib_GetByDefIndex(meleeWep, m_iAttribMelee[client][i]);
				m_flAttrValMelee[client][i] = TF2Attrib_GetValue(attr);
			}
		}
	}
	if (redbots_manager_debug.BoolValue)
	{
		PrintToChatAll("[Command_BoughtUpgrades] SAVED WEAPON STATS FOR %N", client);
	}
	g_bHasBoughtUpgrades[client] = true;
	return Plugin_Handled;
}

// WeaponPoolCount is how many items the pool for that class and slot holds.
//
// It and WeaponPoolAt exist so the loadout menu can walk a pool without repeating
// the class-and-slot chain a third time. The menu used to carry its own copy of
// it, twenty seven blocks of the same four lines; the pools live here, so the way
// into them does too.
stock int WeaponPoolCount(const char[] class, const char[] slot)
{
	if (StrEqual(class, "scout", false))
	{
		if (StrEqual(slot, "primary", false))
		{
			return 31;
		}
		if (StrEqual(slot, "secondary", false))
		{
			return 27;
		}
		if (StrEqual(slot, "melee", false))
		{
			return 27;
		}
	}
	else
		if (StrEqual(class, "soldier", false))
		{
			if (StrEqual(slot, "primary", false))
			{
				return 31;
			}
			if (StrEqual(slot, "secondary", false))
			{
				return 22;
			}
			if (StrEqual(slot, "melee", false))
			{
				return 19;
			}
		}
		else
			if (StrEqual(class, "pyro", false))
			{
				if (StrEqual(slot, "primary", false))
				{
					return 31;
				}
				if (StrEqual(slot, "secondary", false))
				{
					return 21;
				}
				if (StrEqual(slot, "melee", false))
				{
					return 26;
				}
			}
			else
				if (StrEqual(class, "demoman", false))
				{
					if (StrEqual(slot, "primary", false))
					{
						return 17;
					}
					if (StrEqual(slot, "secondary", false))
					{
						return 31;
					}
					if (StrEqual(slot, "melee", false))
					{
						return 24;
					}
				}
				else
					if (StrEqual(class, "heavyweapons", false))
					{
						if (StrEqual(slot, "primary", false))
						{
							return 34;
						}
						if (StrEqual(slot, "secondary", false))
						{
							return 14;
						}
						if (StrEqual(slot, "melee", false))
						{
							return 22;
						}
					}
					else
						if (StrEqual(class, "engineer", false))
						{
							if (StrEqual(slot, "primary", false))
							{
								return 18;
							}
							if (StrEqual(slot, "secondary", false))
							{
								return 23;
							}
							if (StrEqual(slot, "melee", false))
							{
								return 27;
							}
						}
						else
							if (StrEqual(class, "medic", false))
							{
								if (StrEqual(slot, "primary", false))
								{
									return 6;
								}
								if (StrEqual(slot, "secondary", false))
								{
									return 26;
								}
								if (StrEqual(slot, "melee", false))
								{
									return 19;
								}
							}
							else
								if (StrEqual(class, "sniper", false))
								{
									if (StrEqual(slot, "primary", false))
									{
										return 35;
									}
									if (StrEqual(slot, "secondary", false))
									{
										return 19;
									}
									if (StrEqual(slot, "melee", false))
									{
										return 16;
									}
								}
								else
									if (StrEqual(class, "spy", false))
									{
										if (StrEqual(slot, "primary", false))
										{
											return 20;
										}
										if (StrEqual(slot, "secondary", false))
										{
											return 7;
										}
										if (StrEqual(slot, "melee", false))
										{
											return 29;
										}
										if (StrEqual(slot, "pda2", false))
										{
											return 7;
										}
									}
	return 0;
}

// WeaponPoolAt is one item out of that pool.
stock int WeaponPoolAt(const char[] class, const char[] slot, int index)
{
	if (StrEqual(class, "scout", false))
	{
		if (StrEqual(slot, "primary", false))
		{
			return WEAPONS_SCOUT_PRIMARY[index];
		}
		if (StrEqual(slot, "secondary", false))
		{
			return WEAPONS_SCOUT_SECONDARY[index];
		}
		if (StrEqual(slot, "melee", false))
		{
			return WEAPONS_SCOUT_MELEE[index];
		}
	}
	else
		if (StrEqual(class, "soldier", false))
		{
			if (StrEqual(slot, "primary", false))
			{
				return WEAPONS_SOLDIER_PRIMARY[index];
			}
			if (StrEqual(slot, "secondary", false))
			{
				return WEAPONS_SOLDIER_SECONDARY[index];
			}
			if (StrEqual(slot, "melee", false))
			{
				return WEAPONS_SOLDIER_MELEE[index];
			}
		}
		else
			if (StrEqual(class, "pyro", false))
			{
				if (StrEqual(slot, "primary", false))
				{
					return WEAPONS_PYRO_PRIMARY[index];
				}
				if (StrEqual(slot, "secondary", false))
				{
					return WEAPONS_PYRO_SECONDARY[index];
				}
				if (StrEqual(slot, "melee", false))
				{
					return WEAPONS_PYRO_MELEE[index];
				}
			}
			else
				if (StrEqual(class, "demoman", false))
				{
					if (StrEqual(slot, "primary", false))
					{
						return WEAPONS_DEMOMAN_PRIMARY[index];
					}
					if (StrEqual(slot, "secondary", false))
					{
						return WEAPONS_DEMOMAN_SECONDARY[index];
					}
					if (StrEqual(slot, "melee", false))
					{
						return WEAPONS_DEMOMAN_MELEE[index];
					}
				}
				else
					if (StrEqual(class, "heavyweapons", false))
					{
						if (StrEqual(slot, "primary", false))
						{
							return WEAPONS_HEAVY_PRIMARY[index];
						}
						if (StrEqual(slot, "secondary", false))
						{
							return WEAPONS_HEAVY_SECONDARY[index];
						}
						if (StrEqual(slot, "melee", false))
						{
							return WEAPONS_HEAVY_MELEE[index];
						}
					}
					else
						if (StrEqual(class, "engineer", false))
						{
							if (StrEqual(slot, "primary", false))
							{
								return WEAPONS_ENGINEER_PRIMARY[index];
							}
							if (StrEqual(slot, "secondary", false))
							{
								return WEAPONS_ENGINEER_SECONDARY[index];
							}
							if (StrEqual(slot, "melee", false))
							{
								return WEAPONS_ENGINEER_MELEE[index];
							}
						}
						else
							if (StrEqual(class, "medic", false))
							{
								if (StrEqual(slot, "primary", false))
								{
									return WEAPONS_MEDIC_PRIMARY[index];
								}
								if (StrEqual(slot, "secondary", false))
								{
									return WEAPONS_MEDIC_SECONDARY[index];
								}
								if (StrEqual(slot, "melee", false))
								{
									return WEAPONS_MEDIC_MELEE[index];
								}
							}
							else
								if (StrEqual(class, "sniper", false))
								{
									if (StrEqual(slot, "primary", false))
									{
										return WEAPONS_SNIPER_PRIMARY[index];
									}
									if (StrEqual(slot, "secondary", false))
									{
										return WEAPONS_SNIPER_SECONDARY[index];
									}
									if (StrEqual(slot, "melee", false))
									{
										return WEAPONS_SNIPER_MELEE[index];
									}
								}
								else
									if (StrEqual(class, "spy", false))
									{
										if (StrEqual(slot, "primary", false))
										{
											return WEAPONS_SPY_SECONDARY[index];
										}
										if (StrEqual(slot, "secondary", false))
										{
											return WEAPONS_SPY_BUILDING[index];
										}
										if (StrEqual(slot, "melee", false))
										{
											return WEAPONS_SPY_MELEE[index];
										}
										if (StrEqual(slot, "pda2", false))
										{
											return WEAPONS_SPY_PDA2[index];
										}
									}
	return 0;
}

// TimerGiveCustomLoadout puts the chosen weapons in a bot's hands.
//
// A tenth of a second after the spawn rather than on it: the game hands out its
// own items on spawn, and anything given before that is thrown away.
//
// The three weapon entities are kept because the upgrades have to be put back onto
// them afterwards. PDA2 is left out of that: it does not take upgrades.
//
// The attribute fixes are only applied on a bot that has not shopped yet, because
// the upgrade station rewrites them anyway and doing it every spawn is work for
// nothing.
public Action Timer_GiveCustomLoadout(Handle timer, int client)
{
	if (!IsClientInGame(client))
	{
		return Plugin_Stop;
	}
	// These store weapon entity indexes so we can pass them later. PDA2 is
	// excluded as it does not currently get upgrades.
	int primary = -1;
	int secondary = -1;
	int melee = -1;
	if (m_iWeaponPrimary[client] > Go_ItemDefDefault)
	{
		char itemClassname[512];
		bool named = TF2Econ_GetItemClassName(m_iWeaponPrimary[client], itemClassname, 512);
		if (named)
		{
			primary = Go_GiveNamedOrKeepStock(client, TFWeaponSlot_Primary, m_iWeaponPrimary[client], itemClassname, "primary");
		}
		else
		{
			LogError("Timer_GiveCustomLoadout: Could not add primary %d to %N: the schema has no class for it", m_iWeaponPrimary[client], client);
		}
		if ((primary != -1) && !g_bHasBoughtUpgrades[client])
		{
			switch (m_iWeaponPrimary[client])
			{
				case 730:
				{
					// Beggar's Bazooka: prevent overloading.
					TF2Attrib_SetByName(primary, "auto fires when full", 1.0);
				}
				case 996:
				{
					// Loose Cannon: prevent charging.
					TF2Attrib_SetByName(primary, "grenade launcher mortar mode", 0.0);
				}
			}
		}
	}
	if ((m_iWeaponSecondary[client] > Go_ItemDefDefault) && !TF2_IsShieldEquipped(client))
	{
		char itemClassname[512];
		bool named = TF2Econ_GetItemClassName(m_iWeaponSecondary[client], itemClassname, 512);
		if (named)
		{
			secondary = Go_GiveNamedOrKeepStock(client, TFWeaponSlot_Secondary, m_iWeaponSecondary[client], itemClassname, "secondary");
		}
		else
		{
			LogError("Timer_GiveCustomLoadout: Could not add secondary %d to %N: the schema has no class for it", m_iWeaponSecondary[client], client);
		}
		if ((secondary != -1) && !g_bHasBoughtUpgrades[client] && StrEqual(itemClassname, "tf_weapon_pipebomblauncher"))
		{
			// Instant fire stickies.
			TF2Attrib_SetByName(secondary, "stickybomb charge rate", 0.0);
		}
	}
	if (m_iWeaponMelee[client] > Go_ItemDefDefault)
	{
		char itemClassname[512];
		bool named = TF2Econ_GetItemClassName(m_iWeaponMelee[client], itemClassname, 512);
		if (named)
		{
			melee = Go_GiveNamedOrKeepStock(client, TFWeaponSlot_Melee, m_iWeaponMelee[client], itemClassname, "melee");
		}
		else
		{
			LogError("Timer_GiveCustomLoadout: Could not add melee %d to %N: the schema has no class for it", m_iWeaponMelee[client], client);
		}
		if (melee != -1)
		{
			switch (m_iWeaponMelee[client])
			{
				case 1071:
				{
					// A blunt way to check, but these attributes should not be
					// written every spawn.
					if (!g_bHasBoughtUpgrades[client])
					{
						GiveGoldPanStats(melee);
					}
				}
			}
		}
	}
	if (m_iWeaponPDA2[client] > Go_ItemDefDefault)
	{
		char itemClassname[512];
		bool named = TF2Econ_GetItemClassName(m_iWeaponPDA2[client], itemClassname, 512);
		if (named)
		{
			Go_GiveNamedOrKeepStock(client, TFWeaponSlot_Building, m_iWeaponPDA2[client], itemClassname, "pda2");
		}
		else
		{
			LogError("Timer_GiveCustomLoadout: Could not add pda2 %d to %N: the schema has no class for it", m_iWeaponPDA2[client], client);
		}
	}
	if (g_bHasBoughtUpgrades[client])
	{
		ReapplyItemUpgrades(client, primary, secondary, melee);
	}
	//  Certain weapons or upgrades may have changed the health and the ammo
	//
	// 	So both are refilled completely, though the health may end up lower than it
	// 	was if a weapon's attribute lowered the maximum.
	for (int i = TF_AMMO_PRIMARY; i < TF_AMMO_COUNT; i++)
	{
		GivePlayerAmmo(client, 1000, i, true);
	}
	// For players, this is calculated max health.
	int maxHealth = TF2Util_GetEntityMaxHealth(client);
	if (GetClientHealth(client) != maxHealth)
	{
		BaseEntity_SetMaxHealth(client, maxHealth);
		SetEntityHealth(client, maxHealth);
	}
	PostInventoryApplication(client);
	if (redbots_manager_debug.BoolValue)
	{
		PrintToChatAll("[Timer_GiveCustomLoadout] %N's ammo: %d/%d", client, BaseCombatCharacter_GetAmmoCount(client, TF_AMMO_PRIMARY), TF2Util_GetPlayerMaxAmmo(client, TF_AMMO_PRIMARY));
	}
	return Plugin_Stop;
}

// GiveNamedOrKeepStock puts the item in the slot, or leaves the stock one.
//
// The slot used to be emptied before the schema was asked whether the item had a
// class name, and left empty when the give came back with nothing: a Heavy
// joined RED on Rottenburg with no weapon at all, which is mvm-gkv. The callers
// look the name up first now and only reach this with one, and a give that
// fails hands the bot its stock loadout back.
stock int Go_GiveNamedOrKeepStock(int client, int slot, int itemDefinition, char[] itemClassname, const char[] what)
{
	TF2_RemoveWeaponSlot(client, slot);
	TF2Econ_TranslateWeaponEntForClass(itemClassname, 512, TF2_GetPlayerClass(client));
	int weapon = GiveItemToPlayer(client, itemClassname, itemDefinition, 1, 6);
	if (weapon == -1)
	{
		LogError("Timer_GiveCustomLoadout: Could not add %s %d to %N: the give failed, so he keeps stock", what, itemDefinition, client);
		TF2_RegeneratePlayer(client);
	}
	return weapon;
}

