/* Generated by internal/spbody from the Go it is named after. Do not edit. */

// EquipWeaponSlot puts the weapon in that slot in his hands, and answers false
// when the slot is empty.
stock bool EquipWeaponSlot(int client, int slot)
{
	int weapon = GetPlayerWeaponSlot(client, slot);
	if (weapon != -1)
	{
		return TF2Util_SetPlayerActiveWeapon(client, weapon);
	}
	return false;
}

// GetTimeSinceWeaponFired is how long ago he last pulled the trigger, and a very
// long time when he has not or is holding nothing.
stock float GetTimeSinceWeaponFired(int client)
{
	int weapon = BaseCombatCharacter_GetActiveWeapon(client);
	if (weapon == -1)
	{
		return 9999.0;
	}
	float lastFireTime = GetEntPropFloat(weapon, Prop_Send, "m_flLastFireTime");
	if (lastFireTime <= 0.0)
	{
		return 9999.0;
	}
	return GetGameTime() - lastFireTime;
}

// GetMedigunType is which of the four mediguns this is, which the game keeps as a
// weapon mode rather than as an item.
stock int GetMedigunType(int weapon)
{
	return TF2Attrib_HookValueInt(0, "set_weapon_mode", weapon);
}

// GetResistType is which bubble a Vaccinator is on.
stock int GetResistType(int weapon)
{
	return GetEntProp(weapon, Prop_Send, "m_nChargeResistType");
}

// HasSniperRifle says the primary is one, whichever rifle it is.
stock bool HasSniperRifle(int client)
{
	int weapon = GetPlayerWeaponSlot(client, TFWeaponSlot_Primary);
	if (weapon == -1)
	{
		return false;
	}
	return WeaponID_IsSniperRifle(TF2Util_GetWeaponID(weapon));
}

