/gen/
/bin/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
/.gen.first/
/toolchain/

# The agent instructions live here, so this overrides the global ignore of
# CLAUDE.md. Without it every edit to the file is silently uncommitted.
!CLAUDE.md

# What the test-bed measured. The runner writes under the plugin tree, which is
# in this repository now, and a run is evidence for one afternoon rather than
# something to carry.
plugin/results/
/testbed
/rc
